# Server
PORT=3000
NODE_ENV=production

# Security
SESSION_SECRET=change-this-to-a-random-secret-string

# Optional: Base URL for sharing links (set to your VPS domain)
BASE_URL=http://localhost:3000

# Database path (default: ./data/nexus.db)
DB_PATH=./data/nexus.db
